/** Bump when content tables change in a way that affects balance. */
export const CONTENT_VERSION = 1;

/** Bump when the shape of GameState changes; migrations live in engine/state.ts. */
export const SAVE_VERSION = 1;
